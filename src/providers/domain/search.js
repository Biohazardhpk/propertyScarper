const slug = (value) => value.trim().toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
export function domainSearchUrl(c, location, page = 1) {
  const channel = c.transactionType === 'buy' ? 'sale' : c.transactionType === 'sold' ? 'sold-listings' : 'rent'; const q = new URLSearchParams(); if (page > 1) q.set('page', page); if (c.minPrice != null || c.maxPrice != null) q.set('price', `${c.minPrice ?? 0}-${c.maxPrice ?? 'any'}`); if (c.minBedrooms != null) q.set('bedrooms', `${c.minBedrooms}-any`); if (c.minBathrooms != null) q.set('bathrooms', `${c.minBathrooms}-any`); if (c.minCarspaces != null) q.set('carspaces', `${c.minCarspaces}-any`); if (c.minLandAreaM2 != null || c.maxLandAreaM2 != null) q.set('landsize', `${c.minLandAreaM2 ?? 0}-${c.maxLandAreaM2 ?? 'any'}`); if (c.propertyTypes?.length) q.set('ptype', c.propertyTypes.join(',')); if (c.excludeUnderContract) q.set('excludeunderoffer', '1'); if (c.sort === 'newest') q.set('sort', 'dateupdated-desc'); return `https://www.domain.com.au/${channel}/${slug(location)}/?${q}`;
}
